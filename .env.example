# Copy to .env and fill in your key(s).
OPENAI_API_KEY=sk-...
# Optional: Allowed CORS origins (comma-separated). Leave blank to allow all during development.
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
PORT=8787
